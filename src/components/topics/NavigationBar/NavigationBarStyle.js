import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    navigation: {
        position: "absolute",
        left: '10px',
        top: '0px'
    },
    link: {
        textDecoration: 'none'
    }
});

export default { useStyles };
